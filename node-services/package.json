{
  "name": "pocketapp-node-services",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "description": "Standalone Node real-time layer for pocketapp: price-feed broadcast (pricefeed/) and trade-execution socket.io relay (tradesocket/). Own package.json/lockfile deliberately kept separate from the root Vite build.",
  "scripts": {
    "pricefeed": "node pricefeed/index.js",
    "tradesocket": "node tradesocket/index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ioredis": "^5.4.1",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.11.3",
    "socket.io": "^4.8.1",
    "ws": "^8.18.0"
  }
}
